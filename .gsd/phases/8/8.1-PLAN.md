---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Engine Setup & Base Layout

## Objective
Configure the Tailwind CSS foundation and AOS animation library on the global template.

## Context
- `.gsd/SPEC.md`
- `web.txt` (reference)
- `templates/base.html`
- `static/css/style.css`

## Tasks

<task type="auto">
  <name>Migrate Base Template styling to Tailwind</name>
  <files>templates/base.html, static/css/style.css</files>
  <action>
    - Inject `<script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>` into `base.html` head.
    - Inject the tailwind configuration object provided in `web.txt`.
    - Inject the `AOS` (Animate on Scroll) CSS and JS CDNs.
    - Inject the Google Material Symbols font link.
    - Update the top navigation bar and footer in `base.html` to mirror the `.glass-nav` classes, structure, and text styling found in `web.txt`, ensuring our current URLs (`/`, `/analyze`, `/dashboard`, `/history`, `/about`) map to the new items.
    - Transfer custom pseudo-classes (`.glass-nav`, `.floating-blob`, etc.) from `web.txt` into `style.css`. Remove all legacy typography and variable definitions from `style.css` to prevent conflicts.
  </action>
  <verify>Check that `/` loads without broken layouts and imports Tailwind successfully in Network tab.</verify>
  <done>Base layout renders correctly with the sage/glassmorphism design.</done>
</task>

## Success Criteria
- [ ] Tailwind CSS CDN loaded.
- [ ] Base template updated to mirror new design language.
- [ ] Old custom CSS variables removed from `style.css`.
