---
phase: 8
plan: 2
wave: 2
---

# Plan 8.2: Static Content Redesign (Home, About, Contact)

## Objective
Convert `index.html`, `about.html`, and `contact.html` to match the exact aesthetic blocks defined in `web.txt`. Introduce AOS (Animate on Scroll) transitions. Maintain original text.

## Context
- `web.txt`
- `templates/index.html`
- `templates/about.html`
- `templates/contact.html`

## Tasks

<task type="auto">
  <name>Redesign Homepage UI & Animations</name>
  <files>templates/index.html</files>
  <action>
    - Port the Tailwind layout structure from `web.txt` "Homepage".
    - Apply the `.floating-blob` and hero elements to the Jumbotron.
    - Rewrite the "How-to" grid to match the 3-column `surface-container-low` styling in `web.txt`.
    - Apply `data-aos="fade-up"` tags for scroll animations.
    - Preserve all identical current text wordings (e.g. project hero text).
  </action>
  <verify>Render `/` and scroll to confirm styling and animations.</verify>
  <done>Homepage looks exactly like the benchmark inspiration with our custom text.</done>
</task>

<task type="auto">
  <name>Redesign About & Contact pages</name>
  <files>templates/about.html, templates/contact.html</files>
  <action>
    - Revamp `about.html` to mirror the layout of `web.txt` "About VibeCheck" with the Team Profiles (using shadow-ambient classes).
    - Transform `contact.html` form using the Tailwind `forms` plugin input fields (borderless, ringed on focus).
  </action>
  <verify>Test `/about` and `/contact` rendering.</verify>
  <done>Text remains identical but visuals match the benchmark.</done>
</task>

## Success Criteria
- [ ] Homepage animations (AOS) are crisp.
- [ ] 3D elements present in Hero section.
- [ ] "Recent Content" excluded per instructions.
