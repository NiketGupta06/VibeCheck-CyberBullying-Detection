---
phase: 8
plan: 3
wave: 3
---

# Plan 8.3: Functional Mechanics Redesign (Analyze, Dashboard, History)

## Objective
Carry the design system into the app's heavily logic-oriented Views. Maintain all `<form>`, Jinja2 conditional blocks, and script bindings, but change the CSS representations.

## Context
- `web.txt`
- `templates/analyze.html`
- `templates/dashboard.html`
- `templates/history.html`
- `static/js/main.js`

## Tasks

<task type="auto">
  <name>Restyle Analyze Route</name>
  <files>templates/analyze.html</files>
  <action>
    - Refactor the Video Grid & URL input to match `web.txt`'s "Analyze Page" layout, utilizing `bg-surface-container-lowest`.
    - Keep Jinja `{% if user_videos %}` conditioning intact.
    - Update the video card thumbnails utilizing tailwind classes `.aspect-video`, `.editorial-shadow`.
  </action>
  <verify>Check `/analyze` for both logged in and logged out experiences.</verify>
  <done>Styling updated without breaking form submissions.</done>
</task>

<task type="auto">
  <name>Restyle Dashboard and History Data Display</name>
  <files>templates/dashboard.html, templates/history.html, static/js/main.js</files>
  <action>
    - **Dashboard**: Use `.stat-card-tilt:hover` on the top metric cards. Restructure the comment row logic `{% for item in ... %}` to use Tailwind table rows. Redesign the Chart.js visual containers to `bg-surface-container-low`.
    - Modify the Chart.js configs in `main.js` to replace neon accent colors with the new `--primary` hex `#4c6647`.
    - **History**: Redesign the History table applying `.hover:bg-primary-container/30` logic.
  </action>
  <verify>Run an analysis and ensure graphs and grids render within bounds.</verify>
  <done>Data flows flawlessly into the new sophisticated layout.</done>
</task>

## Success Criteria
- [ ] Video analysis mechanics unmodified but visually upgraded.
- [ ] Stat metrics utilize 3D card tilt effects.
- [ ] Charts match the sage green color palette.
